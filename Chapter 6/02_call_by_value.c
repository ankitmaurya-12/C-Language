#include<stdio.h>
int sum (int x, int y);

int main(){
    int a, b;
    printf("The sum of two no. is %d\n",sum(3,4));
    return 0;
}
int sum (int x, int y){
    int result;
    result = x +y;
    x=45697;
    y=45136;
    return result;
}