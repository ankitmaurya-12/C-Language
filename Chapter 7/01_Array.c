#include<stdio.h>

int main(){
    // suppose we have to enter marks of 90 students
    // Naive to enter the marks is below:-
    // int mark1,mark2,mark3,mark4;
    // mark1=65;
    // mark2=75;
    // mark3=55;
    // mark4=45;

// Array is collection of similar items
    int marks[4]; // in between [ ] we write how many variable we want
    marks[0]=65; // it always satrt from 0 in c and goes to n-1
    marks[1]=75;
    marks[2]=55;
    marks[3]=45;

    char name[20]; //Character Array => it store 20 character, group of character called string ANKIT
    float percentile[30]; //Float Array => it will store 30 percentile value
    return 0;
}